# Personal/private side project — use the public npm registry directly.
# (Apple's npm.apple.com mirror is allowlisted and doesn't carry many packages.)
registry=https://registry.npmjs.org/

# Don't let pnpm auto-fetch a different version of itself.
manage-package-manager-versions=false
