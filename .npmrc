# Inherits registry + auth from user's ~/.npmrc (Apple npm.apple.com).
# Project-level overrides only:

# Don't let pnpm auto-fetch a different version of itself
# (the @pnpm/exe scope isn't carried by Apple's mirror).
manage-package-manager-versions=false
